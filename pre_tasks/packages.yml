---
- name: apt cache update & upgrade
  apt:
    update_cache: true
    cache_valid_time: 3600
    upgrade: full

- name: apt install packages
  apt:
    name:
      - curl
      - git
    state: present
    cache_valid_time: 3600
