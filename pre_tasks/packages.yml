---
- name: apt cache update & upgrade
  apt:
    update_cache: true
    cache_valid_time: 3600
    upgrade: full

- name: install git and curl
  apt:
    name:
      - curl
      - git
    state: present
    cache_valid_time: 3600
